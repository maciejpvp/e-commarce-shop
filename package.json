{
  "scripts": {
    "test": "jest",
    "format:tf": "terraform -chdir=infra fmt -recursive",
    "tf:dev:init": "terraform -chdir=infra init && (terraform -chdir=infra workspace select dev || terraform -chdir=infra workspace new dev)",
    "tf:dev:plan": "terraform -chdir=infra workspace select dev && terraform -chdir=infra plan -var='Environment=dev'",
    "tf:dev:deploy": "terraform -chdir=infra workspace select dev && terraform -chdir=infra apply -var='Environment=dev' -auto-approve",
    "tf:staging:init": "terraform -chdir=infra init && (terraform -chdir=infra workspace select staging || terraform -chdir=infra workspace new staging)",
    "tf:staging:plan": "terraform -chdir=infra workspace select staging && terraform -chdir=infra plan -var='Environment=staging'",
    "tf:staging:deploy": "terraform -chdir=infra workspace select staging && terraform -chdir=infra apply -var='Environment=staging' -auto-approve",
    "tf:prod:init": "terraform -chdir=infra init && (terraform -chdir=infra workspace select prod || terraform -chdir=infra workspace new prod)",
    "tf:prod:plan": "terraform -chdir=infra workspace select prod && terraform -chdir=infra plan -var='Environment=prod'",
    "tf:prod:deploy": "terraform -chdir=infra workspace select prod && terraform -chdir=infra apply -var='Environment=prod' -auto-approve"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.992.0",
    "@aws-sdk/client-s3": "^3.993.0",
    "@aws-sdk/client-ses": "^3.1004.0",
    "@aws-sdk/client-sfn": "^3.1000.0",
    "@aws-sdk/client-ssm": "^3.1000.0",
    "@aws-sdk/lib-dynamodb": "^3.992.0",
    "@aws-sdk/s3-presigned-post": "^3.993.0",
    "aws-jwt-verify": "^5.1.1",
    "aws-lambda": "^1.0.7",
    "joi": "^18.0.2",
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "stripe": "^20.4.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.2.3",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "esbuild": "^0.27.3",
    "i": "^0.3.7",
    "jest": "^30.3.0",
    "npm": "^11.10.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.8.3"
  }
}